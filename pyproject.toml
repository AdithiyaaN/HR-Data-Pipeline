[project]
name = "job-pipeline"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "openai",
    "pandas",
    "streamlit",
]

[project.optional-dependencies]
dev = [
    "hypothesis",
    "pytest",
    "pytest-mock",
]
